interface ISelectionDot {
  active: boolean;
}

const SelectionDot = ({ active }: ISelectionDot) => {
  // TODO: Make it selection dot.

  return (
    <div>
      <div />
    </div>
  );
};

export default SelectionDot;
